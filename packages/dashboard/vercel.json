{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "tsc -b && vite build --outDir dist --emptyOutDir",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
